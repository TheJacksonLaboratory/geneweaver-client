[tool.poetry]
name = "jax-geneweaver-client"
version = "0.0.1"
description = ""
authors = ["Jax & Baylor Computational Sciences <cssc@jax.org>"]
readme = "README.md"
packages = [
    { from="src", include= "jax/geneweaver/client"}
]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.1"
pydantic = {extras = ["dotenv"], version = "^1.10.2"}
jax-geneweaver-core = "^0.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
